# Active Context

Updated: 2026-08-23

## Current focus
Memory bank initialised for the local-ai-dev-workflow-voting-app.

## Recent changes
Scaffolded `memory-bank/` and `docs/decisions/` via `init-memory-bank.sh`; inferred stack, commands, and scope from README, `.workflow/requirements.md`, and `scripts/`.

## Next step
Run `./scripts/deploy.sh` then `./scripts/verify.sh` against a live k3d cluster to confirm R1–R17.

## Active decisions
none

## Blocked
none
